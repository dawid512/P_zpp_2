@model P_zpp_2.Models.MyCustomLittleDatabase.ScheduleInstructions

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="cardlogged">
    <div class="card-header">
        <h2>Usuń</h2>

        <h3>Czy napewno chcesz usunąć?</h3>
        <div>
            <h4>Instrukcje</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2 napis-label">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-10 napis-label">
                    @Html.DisplayFor(model => model.Name)
                </dd>
                <dt class="col-sm-2 napis-label">
                    @Html.DisplayNameFor(model => model.ListOfShistsInJSON)
                </dt>
                <dd class="col-sm-10 napis-label">
                    @Html.DisplayFor(model => model.ListOfShistsInJSON)
                </dd>
                <dt class="col-sm-2 napis-label">
                    @Html.DisplayNameFor(model => model.Coordinator)
                </dt>
                <dd class="col-sm-10 napis-label">
                    @Html.DisplayFor(model => model.Coordinator.Id)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <input type="submit"  value="Usuń" class=" btn   login_btn napis-label " /> 
                <a class=" btn float-md-right  login_btn napis-label" asp-action="Index">Wróć do Listy</a>
            </form>
        </div>
